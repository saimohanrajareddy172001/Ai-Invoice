# Secrets — NEVER commit these
.env
credentials.json
token.json
# get-refresh-token.mjs has hardcoded OAuth client secret — keep out of git
get-refresh-token.mjs

# Apify local storage (contains INPUT.json with plaintext credentials)
storage/

# Temp files
.tmp/

# One-off debug/maintenance scripts — not part of production build
fix-*.mjs
inspect-*.mjs

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store

# Dependencies
node_modules/
.venv
